# Caddy 片段模板 —— TLS 终结 + 反代到本机 dashboard
#
# 与 frp 的关系:两者是【并列的两个公网入口】,都终结在同一个
# 127.0.0.1:3001 上。判断两个入口是不是同一实例的方法:
# 比对它们返回的同一个静态文件的 Last-Modified —— 精确到秒相同
# 就是同一份磁盘上的同一个文件,不可能是两台机器各自安装的。
#
# 🔴 填好域名的那份不必然含密钥,但仍建议只在部署机上维护。

${PUBLIC_DOMAIN}:3000 {
    reverse_proxy 127.0.0.1:3001
}
