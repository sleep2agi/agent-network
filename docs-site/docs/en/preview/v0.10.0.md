# v0.10.0 Release Notes — Direct Runtime + Observability Foundations

> **Phase 1 latest LIVE (2026-05-16)**: `@sleep2agi/agent-network@2.2.0`, `@sleep2agi/agent-node@2.4.0`, `@sleep2agi/commhub-server@0.8.2` shipped via npm `latest` tag; dashboard `0.5.0` Phase 2 will promote after the §3.D/F/G surfaces ship. Full changes in [changelog v0.10.0](/en/changelog#v0-10-0-direct-runtime-observability-foundations-2026-05-16-stable-phase-1-3-package-promote).

::: tip ✅ v0.10.0 stable Phase 1 shipped
This page is kept as a v0.10.0 release-notes overview. Install / upgrade:

```bash
npm install -g @sleep2agi/agent-network@latest   # install
anet upgrade                                     # upgrade + restart project
```

A dashboard upgrade section will be added here after dashboard `0.5.0` Phase 2 promotes.
:::

## 🎯 Theme

v0.7 → v0.9.2 accumulated 11 releases; the 5-P0 ripple chain (#135-#139) surfaced a runtime architecture debt. v0.10.0's theme is **fix the root** — runtime debt (codex-sdk wrapper bypass) + observability foundations (per-server daemon scaffold + per-agent token telemetry). This is the groundwork for v0.11.0's 24/7 multi-vendor AI Agent society live stream.

## Stable versions (Phase 1)

| Package | latest |
|---|---|
| `@sleep2agi/agent-network` | `2.2.0` ✅ |
| `@sleep2agi/agent-node` | `2.4.0` ✅ |
| `@sleep2agi/commhub-server` | `0.8.2` ✅ |
| `@sleep2agi/agent-network-dashboard` | `0.4.6` (promotes to `0.5.0` after Phase 2) |

## 3 Hero P0s

### Hero 1 — Server health monitoring ([#99](https://github.com/sleep2agi/agent-network/issues/99) Phase 1)

Per-server daemon scaffold, monitoring-only. New endpoints:

- `GET /api/server/:host/health` — CPU / mem / disk / process health metrics
- History table `agent_telemetry` (commhub-server schema migration auto-creates on first boot; retention policy is being finalized in the preview chain)

Dashboard integration: [ServersDrawer (#119)](https://github.com/sleep2agi/agent-network/issues/119) gets a "Server health" tab. The control layer (kill / restart / redeploy) is deferred to v0.11.0.

### Hero 2 — Agent list per server ([#99](https://github.com/sleep2agi/agent-network/issues/99) endpoint family)

- `GET /api/server/:host/agents` — live agent list + historical telemetry per host

Same endpoint family as Hero 1, shipped in the same cycle. Lets the dashboard render the active agents on each machine plus per-agent health.

### Hero 3 — TopoGraph upgrade (placeholder)

Network topology visualization upgrade. RFC outline is still pending approval — scope will be filled in once the RFC is ready (candidate directions: node aggregation, edge weighting, history replay).

Tracking: [RFC-012 TopoGraph Upgrade (draft pending)](https://github.com/sleep2agi/agent-network/issues/107)

## Companion P0 — Per-agent process telemetry ([#142](https://github.com/sleep2agi/agent-network/issues/142))

::: tip T2.1 shipped in preview.1
agent-node now embeds its own process telemetry in every `commhub_report_status` heartbeat. Wires directly into the Hero 1/2 endpoint family.
:::

Fields agent-node reports (under the `process_telemetry` sub-object in the `commhub_report_status` payload):

| Field | Value | Source |
|---|---|---|
| `rss` | bytes | `process.memoryUsage().rss` |
| `cpu_pct` | % over interval | `process.cpuUsage()` delta / wall delta |
| `uptime_seconds` | s | `process.uptime()` |
| `in_flight_count` | int | counter wrapping `send_task` / `turn/start`, decremented on completion |

Zero sysmon dependency, zero privilege required, zero platform-specific code — sampled at heartbeat interval and appended to the existing `report_status` payload.

### 4-phase ship plan

| Phase | Content | Ship status |
|---|---|---|
| T2.1 | `agent-node/src/telemetry.ts`: sample at heartbeat interval, append to `report_status` payload | ✅ preview.1 (`@sleep2agi/agent-node@2.3.11-preview.1`) |
| T2.2 | commhub-server schema: accept `process_telemetry` field, aligned with the T2.1 wire shape | ✅ preview.5 (`@sleep2agi/commhub-server@0.8.1-preview.5`, commit [209cac7](https://github.com/sleep2agi/agent-network/commit/209cac7)) |
| T2.3 | Dashboard sidebar / hover card: render telemetry with real-time SSE updates | 🟡 preview.N pending |
| T2.4 | Docker + pexpect chained smoke: host + agent telemetry both report cleanly, dashboard renders both layers | 🟡 pending T2.3 |

Sibling to [#119](https://github.com/sleep2agi/agent-network/issues/119) host fields (step 1 host layer shipped; agent layer step 2 now in progress).

## New runtime path: `codex-direct-stdio`

::: tip Shipped in preview.1
`@sleep2agi/agent-node@2.3.11-preview.1` — codex app-server stdio direct, Phase 1.1-1.4 + AskForApproval enum kebab-case wire-convention fix
:::

The codex runtime used to go through the `@openai/codex-sdk` npm wrapper. The wrapper's `--mcp-config` HTTP transport bug is the root cause family of the [#102](https://github.com/sleep2agi/agent-network/issues/102) hang. v0.10.0 replaces it with `spawn('codex', ['app-server'])` + a minimal stdio JSON-RPC client, **bypassing the wrapper entirely** — that family bug is sidestepped, and we get the full 67-method v2 protocol surface (thread / turn / item / realtime / ...) instead of being limited by codex-sdk breaking changes.

### Naming

| Field | Value |
|---|---|
| Runtime ID | `codex-direct-stdio` |
| `anet node create --runtime` value | `codex-direct-stdio` |
| Legacy fallback | `ANET_CODEX_LEGACY_SDK=1` env var falls back to the old wrapper (safe rollback for the preview chain) |

### 4-phase ship plan (per [#141](https://github.com/sleep2agi/agent-network/issues/141))

| Phase | Content | Ship status |
|---|---|---|
| 1.1 | Vendor ~50 .ts files from codex's `schema/typescript/v2/` into `agent-node/src/types/codex/` (auto-generated, zero hand-writing) | ✅ preview.0/1 |
| 1.2 | Minimal stdio JSON-RPC client (~80 LOC): request/response id correlation, notification dispatcher (EventEmitter), `-32600` error envelope | ✅ preview.0/1 |
| 1.3 | Rewrite `agent-node/src/runtime/codex.ts` to spawn directly; map anet task → codex `thread/start` + `turn/start`, stream `item/agentMessage/delta` → anet task update | ✅ preview.0/1 |
| 1.4 | 5+ case Docker + pexpect real-TTY chained smoke: initialize / thread / turn-with-prompt / CollabAgentToolCall ↔ anet send_task / graceful shutdown | ✅ preview.0/1 |
| 1.5 | Replay historical #102 hang scenario, verify resolved, comment + close [#102](https://github.com/sleep2agi/agent-network/issues/102) / [#103](https://github.com/sleep2agi/agent-network/issues/103) | 🟡 preview.N pending |

### User switchover

Existing codex nodes are **not auto-migrated** during the preview chain. Pick `codex-direct-stdio` explicitly for new nodes:

```bash
anet node create --runtime codex-direct-stdio --vendor anthropic ...
```

After stable promote, `codex-direct-stdio` becomes the default codex runtime; the old `codex-sdk` path enters deprecation warning. v0.11.0 plans to remove the wrapper path.

### Roll back to the wrapper (preview-window safe rollback)

```bash
ANET_CODEX_LEGACY_SDK=1 anet node start <codex-node>
```

Preview-window only. If you hit a bug, please file on [Issue tracker](https://github.com/sleep2agi/agent-network/issues) — don't run the fallback as a long-lived production path.

## Release cadence (per [methodology §2.0](https://github.com/sleep2agi/agent-network/blob/main/docs/sop/methodology.md))

| Stage | Status |
|---|---|
| 1. Issue planning | ✅ [#140](https://github.com/sleep2agi/agent-network/issues/140) |
| 2. preview chain | ✅ agent-node `preview.0/1` + commhub-server `preview.4/5` bundle #141 Phase 1.1-1.4 + #99 endpoints + #142 T2.1/T2.2 |
| 3. Clean version → promote latest | ✅ agent-network 2.2.0 / agent-node 2.4.0 / commhub-server 0.8.2 |
| 4. GitHub Release v0.10.0 EN + ZH | 🟡 pending (fires after this release-notes lands) |
| 5. anet.sh stable docs swap (mandatory) | ✅ done this round (CHANGELOG / README / version switcher / this page) |
| 6. Post-promote verify + retro | 🟡 in-progress (curl verify follows this deploy; dashboard `0.5.0` Phase 2 retro afterwards) |

## Related issues

- [#140](https://github.com/sleep2agi/agent-network/issues/140) — v0.10.0 release tracker
- [#141](https://github.com/sleep2agi/agent-network/issues/141) — codex app-server stdio direct (P0, Track 1)
- [#99](https://github.com/sleep2agi/agent-network/issues/99) — Per-server daemon Phase 1 scaffold (P0, Track 2)
- [#142](https://github.com/sleep2agi/agent-network/issues/142) — Per-agent process telemetry (P0, Track 2 sibling, T2.1 ✅ preview.1)
- [#114](https://github.com/sleep2agi/agent-network/issues/114) — Per-agent token usage telemetry (P1)
- [#119](https://github.com/sleep2agi/agent-network/issues/119) — ServersDrawer (#99 integration ref)
- [#102](https://github.com/sleep2agi/agent-network/issues/102) / [#103](https://github.com/sleep2agi/agent-network/issues/103) — close as a side-effect of the new runtime

## Feedback

Preview-stage bugs / behavior weirdness: [GitHub Discussions](https://github.com/sleep2agi/agent-network/discussions). Security: [Security Advisories](https://github.com/sleep2agi/agent-network/security/advisories/new).
