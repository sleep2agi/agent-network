# v0.10.0 Preview — Direct Runtime + Observability Foundations

> 仍在 preview 阶段。preview.0 已 ship：`@sleep2agi/agent-node@2.3.11-preview.0`（codex app-server stdio Phase 1.1-1.4），commhub-server `GET /api/server/:host/health` + `GET /api/server/:host/agents` endpoints scaffolded。
>
> stable docs swap 在 v0.10.0 release ship 后（per [methodology §2.0 Stage 5](https://github.com/sleep2agi/agent-network/blob/main/docs/sop/methodology.md)）。

::: warning Preview, 不 stable
v0.10.0 仍在 preview chain 迭代，本页是 Stage 2 preview docs outline。行为 / 命名 / endpoint shape 在 stable promote 前都可能调整，不要把生产链路绑死。stable 路径继续走 [v0.9.2](/changelog#v0-9-2-2026-05-16-stable)。
:::

## 🎯 主题

v0.7 → v0.9.2 累积 11 release、5 P0 chain（#135-#139）暴露 runtime 架构债。v0.10.0 主题是 **治本** —— runtime 架构债（codex-sdk wrapper bypass）+ observability 基础（守护节点 scaffold + per-agent token 统计）。为 v0.11.0 多厂商 AI Agent 社会 24/7 直播打地基。

## Target versions

| 包 | preview 起步 | 目标 stable |
|---|---|---|
| `@sleep2agi/agent-network` | tracking preview | `2.2.0` |
| `@sleep2agi/agent-node` | `2.3.11-preview.0` | `2.4.0` |
| `@sleep2agi/commhub-server` | tracking preview | `0.8.2` |
| `@sleep2agi/agent-network-dashboard` | tracking preview | `0.4.7+` |

实际 preview 版本以 `npm view @sleep2agi/agent-node@preview version` 为准（preview chain 滚动更新，避免本页 hardcode 一致性问题）。

## 3 Hero P0

### Hero 1 — 服务器健康监测（[#99](https://github.com/sleep2agi/agent-network/issues/99) Phase 1）

per-server daemon scaffold，监测层 only。新增 endpoint：

- `GET /api/server/:host/health` — CPU / mem / disk / process 健康指标
- 历史表 `agent_telemetry`（commhub-server schema migration 首次 boot 自动创建，retention 策略 preview.N 内回退确认）

Dashboard 集成：[ServersDrawer (#119)](https://github.com/sleep2agi/agent-network/issues/119) 加 "服务器健康" tab。控制层（kill / restart / redeploy）留 v0.11.0。

### Hero 2 — Agent list per server（[#99](https://github.com/sleep2agi/agent-network/issues/99) endpoint family）

- `GET /api/server/:host/agents` — agent list per server + 历史 telemetry

跟 Hero 1 同 endpoint family，同期 ship。给 dashboard 渲染每台机器上的活跃 agent 列表 + per-agent 健康指标。

### Hero 3 — TopoGraph 升级（placeholder）

network topology 可视化升级，仍在 RFC outline approve 阶段。RFC ready 后回填 scope（节点聚合、边权重、history replay 等候选方向待定）。

跟踪：[RFC-012 TopoGraph Upgrade（草案 pending）](https://github.com/sleep2agi/agent-network/issues/107)

## 新 runtime 路径：`codex-direct-stdio`

::: tip preview.0 已 ship
`@sleep2agi/agent-node@2.3.11-preview.0` — codex app-server stdio direct Phase 1.1-1.4
:::

之前 codex runtime 走 `@openai/codex-sdk` npm wrapper，wrapper 的 `--mcp-config` HTTP transport bug 是 [#102](https://github.com/sleep2agi/agent-network/issues/102) hang root cause family。v0.10.0 改成 `spawn('codex', ['app-server'])` + 最小 stdio JSON-RPC client，**bypass wrapper 整个绕过这 family bug**，同时拿到完整 67-method v2 protocol surface（thread / turn / item / realtime / 等），不再受 codex-sdk breaking change 牵制。

### 命名

| 字段 | 值 |
|---|---|
| Runtime ID | `codex-direct-stdio` |
| `anet node create --runtime` value | `codex-direct-stdio` |
| Legacy fallback | `ANET_CODEX_LEGACY_SDK=1` env var 回到旧 wrapper（safe rollback for preview chain）|

### 4 Phase ship 路线（per [#141](https://github.com/sleep2agi/agent-network/issues/141)）

| Phase | 内容 | Ship 状态 |
|---|---|---|
| 1.1 | vendor codex `schema/typescript/v2/` ~50 .ts 文件到 `agent-node/src/types/codex/`（auto-generated, 零手写） | ✅ preview.0 |
| 1.2 | minimal stdio JSON-RPC client ~80 LOC：request/response id 关联 + notification dispatcher (EventEmitter) + `-32600` error envelope | ✅ preview.0 |
| 1.3 | 改写 `agent-node/src/runtime/codex.ts` 走直 spawn；map anet task → codex `thread/start` + `turn/start`，stream `item/agentMessage/delta` → anet task update | ✅ preview.0 |
| 1.4 | 5+ case Docker + pexpect 真 TTY 链式 smoke：initialize / thread / turn-with-prompt / CollabAgentToolCall ↔ anet send_task / graceful shutdown | ✅ preview.0 |
| 1.5 | 历史 #102 hang 场景回归 verify，comment + close [#102](https://github.com/sleep2agi/agent-network/issues/102) / [#103](https://github.com/sleep2agi/agent-network/issues/103) | 🟡 preview.N pending |

### 用户切换

preview chain 期间，现有 codex node **不自动 migrate**。新建节点显式选 `codex-direct-stdio`：

```bash
anet node create --runtime codex-direct-stdio --vendor anthropic ...
```

stable promote 后 `codex-direct-stdio` 替换成默认 codex runtime，旧 `codex-sdk` 路径进入 deprecation warning。v0.11.0 计划移除 wrapper 路径。

### 切回 wrapper（preview 期 safe rollback）

```bash
ANET_CODEX_LEGACY_SDK=1 anet node start <codex-node>
```

只针对 preview 阶段；遇 bug 优先按 [Issue tracker](https://github.com/sleep2agi/agent-network/issues) 上报，不靠 fallback 长期跑生产。

## Preview 节奏（per [methodology §2.0](https://github.com/sleep2agi/agent-network/blob/main/docs/sop/methodology.md)）

| Stage | 状态 |
|---|---|
| 1. Issue 规划 | ✅ [#140](https://github.com/sleep2agi/agent-network/issues/140) ship |
| 2. preview.0 → preview.N | 🟡 in-progress（preview.0 ship 2026-05-16）|
| 3. clean version promote latest | pending |
| 4. GitHub Release v0.10.0 EN + ZH | pending |
| 5. anet.sh stable docs swap（强制） | pending |
| 6. post-promote verify + retro | pending |

## 相关 issue

- [#140](https://github.com/sleep2agi/agent-network/issues/140) — v0.10.0 release tracker
- [#141](https://github.com/sleep2agi/agent-network/issues/141) — codex app-server stdio direct (P0)
- [#99](https://github.com/sleep2agi/agent-network/issues/99) — 守护节点 Phase 1 scaffold (P0)
- [#114](https://github.com/sleep2agi/agent-network/issues/114) — Agent Token 使用量统计 (P1)
- [#119](https://github.com/sleep2agi/agent-network/issues/119) — ServersDrawer (#99 整合 ref)
- [#102](https://github.com/sleep2agi/agent-network/issues/102) / [#103](https://github.com/sleep2agi/agent-network/issues/103) — 顺带关掉

## 反馈渠道

preview 阶段 bug / 行为反常：[GitHub Discussions](https://github.com/sleep2agi/agent-network/discussions)。安全相关：[Security Advisories](https://github.com/sleep2agi/agent-network/security/advisories/new)。
