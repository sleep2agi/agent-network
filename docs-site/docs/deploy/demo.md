# Demo 已合并到案例库

Demo 不再在部署区单独维护。**当前可运行案例的完整列表见 [案例与 Demo](/cases/)** —— 那里是唯一的 single source of truth，新案例只在那边加，避免本页表格 drift。

保留这个页面是为了兼容旧链接。部署 CommHub 或 npm 包请继续看 [Docker 部署](/deploy/docker) 和 [npm 部署](/deploy/npm)。
