# test598 — Codex co-presence single-node normalizer

Date: 2026-08-08 Asia/Shanghai
Source commits: 1087650e81cc438da24820b66509b50486182bb4 + TUI follow-up c754b639c95b76d1a07403e5fd1797ef9224d6fa + owner/socket gates 21e4dadcfa8350b549733e66e8f31af1504d9f12 + explicit permission preparation 236c73a8b74e026a0fe96485814334e989c5b048 + explicit model pin dfbd2fb0a0ce1864bf71e1c53adc8cd1fe662879 + workdir permission preparation 6cf5c7894760be13ac0cadc1e36f6bd0a91c986f + exact TUI process-tree socket verification 8481306b595a83356da6a0d4743c520de18f687c
Image: anet-test598:dev
Image ID: sha256:c0fc0d934993f71f7be12af3f5140c2dfbd11c6a441098f9799084ce73b2dc8b
Scope: Docker isolation only; no production node, Hub, Dashboard, bridge, TUI, or app-server was mutated.

## Result

PASS.

- L0: Bash/JavaScript build and plan-only non-mutation passed.
- L1: owner/type/mode validation, permission repair, per-node goalsPath, duplicate node_id, duplicate thread ID, and duplicate goalsPath passed.
- L1 also proves single-node apply cannot implicitly chmod the fleet inventory: it fails before stopping any runtime until a separately reviewed `prepare-permissions` step has completed.
- L2: a real isolated tmux server stopped three exact PID+starttime-owned sessions, started a fake app-server, bridge, and TUI, verified the pinned executable version, persisted the exact `anet:<node_id>:<alias>` thread owner, verified the real cwd, proved a child in the exact TUI pane process tree held an ESTAB socket to that app-server, and left the goal store absent (no legacy migration).
- L2 migrated the selected config from stale `gpt-5.5` to the operator-pinned `gpt-5.6-sol`; an injected later startup failure restored the byte-identical stale-model fixture, proving the model update participates in rollback.
- L3: thread-owner mismatch, thread-cwd mismatch, PID drift, unaccounted alias process, injected post-start version failure, injected TUI exit, and a live TUI process tree with no app-server socket all failed closed. A mutation that weakened the socket test to the pane root PID rejected the valid child-held socket and turned red. Injected rollout failures restored the byte-identical config, removed the new empty goals directory, and left no new app-server/bridge/TUI sessions.
- L4: removing the exact goalsPath collision predicate was witnessed red; removing/weakening process and rollback anchors remains covered by the explicit negative paths above.
- Secret gate: the report contains no test token value; runtime output exposes only command hashes.

Witnessed-red results:

- non-owner-controlled: rc=2
- implicit fleet permission repair: rc=2
- normalizer apply with pending permission repairs, old sessions preserved: rc=2
- duplicate-node: rc=2
- duplicate-goals-path: rc=2
- duplicate-thread: rc=2
- collision mutation: rc=1
- unaccounted process: rc=2
- thread-owner mismatch: rc=2
- thread-cwd mismatch: rc=2
- PID birth-time drift: rc=2
- app-server version failure with rollback: rc=1
- TUI exit with all-component rollback: rc=1
- live TUI without app-server socket with all-component rollback: rc=1
- root-pane-only socket mutation against a valid child-held socket: rc=1

Exact-source Docker run:

- source commit embedded in the test image: `8481306b595a83356da6a0d4743c520de18f687c`
- positive socket evidence: `TUI_SOCKET pane_pid=692 holder_pid=709 descendants=2`
- final result: `RESULT: PASS`

Real production read-only socket ownership evidence:

- `通信牛`: the tmux pane process is Node, while its native Codex child holds the ESTAB connection to app-server port 24701
- `TM副责人`: the tmux pane process is Node, while its native Codex child holds the ESTAB connection to app-server port 24702
- conclusion: checking only the pane root PID is over-strict and would safely but incorrectly roll back a healthy rollout; the gate must enumerate only that exact pane process tree and accept the exact child holder

Real Codex 0.147 model probe (fully isolated alias/app-server/workspace; no production config or session):

- `/proc/<listener-pid>/exe --version` reported `0.147`
- model `gpt-5.6-sol` completed one real turn in approximately 5.4 seconds
- conclusion: normalizing a node may retain `gpt-5.6-sol`; restarting its app-server naturally moves it from the observed 0.144/0.145 binaries to 0.147

Real Codex 0.147 TUI behavior probe (isolated port 24753, no Hub/MCP):

- persistent fresh thread created with one `READY` bootstrap turn
- before app-server stop: `pane_dead=0`, process `node`
- five seconds after exact app-server stop: `pane_dead=1`, exit status `1`
- conclusion: a remote TUI does not reconnect by itself; rollout must restart all three components

Production-shaped read-only plan against `设计副责人`:

- inventory configs discovered: 20
- canonical goalsPath: `/home/vansin/rfc030-work/.demo/goals/n_b3fac24e/goals.json`
- legacy store: absent; no migration planned
- unique inventory permission repairs reported, not applied: 21 (20 configs `0664→0600` + inventory directory `0775→0700`)

Production-shaped read-only pilot inspection against `P站副责人`:

- Hub status reports `gpt-5.6-sol`, while its bridge config still contains stale `gpt-5.5`
- a legacy `codex-sdk` owner, legacy bridge, and app-server are simultaneously present; the plain alias tmux is not a shared TUI
- its workdir is `0775`; the explicit preparation plan therefore contains 22 unique paths total (the 21 fleet inventory paths plus this selected workdir `0775→0700`)
- the read-only normalizer plan accounted for all five matching processes (three tmux owners plus two children); the old native Codex listener executable is already marked deleted while still running
- the normalizer now requires `--expected-model gpt-5.6-sol`, reports `modelFrom/modelTo`, and atomically pins that model instead of preserving the stale bridge snapshot

The candidate is not a deployment authorization. A real-node apply still requires an owner-selected interruptible node/window, exact process/session coordinates for all three components, external socket single-owner verification, and `/goal`, `/loop` notice, `/aloop` create/cancel UAT.

## Production pilot — `P站副责人`

The owner authorized continued rollout on 2026-08-08. The pilot was limited to
one interruptible, unattached node. Hub, Dashboard, and every other node were
left running.

Artifact and preflight:

- runtime source was the exact `origin/main` commit `0d2e08a1af8daa1cf0cb607f594debb1f0eb1bce`; the normalizer branch changes no `agent-node` runtime source relative to that commit
- staged bridge bundle: `/home/vansin/.local/share/agent-network/candidates/0d2e08a1/agent-node-cli.js`
- staged bridge SHA-256: `f6f05ecc93854e9025f0616041ee75e407dc6135ae73e0212345a0c298b1be6e`
- fresh preflight accounted for all five old matching processes and all three exact tmux owners; the selected TUI had zero attached clients
- plan enumerated the complete 20-config inventory and 22 explicit permission repairs
- `prepare-permissions` changed only modes: the aggregate hash of all 20 config files was identical before and after (`de444f4aee2e9dfc737234ed86595c33e667e47818cc8c4c13b54bc8280f0dec`)

Apply result:

- `RESULT: NORMALIZED alias=P站副责人 appsrv_version=0.147.0`
- rollback coordinates: `/tmp/anet-normalize-n_b4b7a48d.cCobql`
- config model changed from stale `gpt-5.5` to `gpt-5.6-sol`; `flags.model` is absent
- canonical goal store is `/home/vansin/rfc030-work/.demo/goals/n_b4b7a48d/goals.json`
- persisted thread owner verifies as `anet:n_b4b7a48d:P站副责人`, with cwd `/home/vansin/p-station`
- old process IDs are absent; the surviving topology is exactly app-server wrapper/native child, bridge, and TUI wrapper/native child
- the native TUI child, not the pane root, owns the ESTAB socket to port 24717
- final config/goal files are mode 0600; inventory, goals directories, and workdir are mode 0700

Production UAT:

- baseline network task produced one exact reply; the pre-existing agent-to-agent parent lifecycle leak was not hidden, and its test row was explicitly cancelled after reply delivery
- authenticated Dashboard `/goal` task `2ff95a4d-2d49-4af3-9c7e-561179f1090e` entered the shared thread, returned a substantive reply, and left the goal store absent
- authenticated Dashboard `/loop 5m` task `d6e93a12-ff0b-4c76-bd82-abe3f8cb3ac0` entered the shared thread, prepended the `/aloop` migration notice, and left the goal store absent
- authenticated Dashboard `/aloop 1h` task `7fc48d89-3945-41c5-aae6-1778fd43cec6` created goal `0b8f0f9c-ad95-4562-a95f-f1fcd7bf653f`; the goal was then atomically cancelled and the bridge restarted to reload the terminal state
- a node-token request that forged `meta.auth_origin=user` was rewritten by Hub to `auth_origin=node`, used legacy scheduler compatibility, and prepended the migration notice; its test goal was cancelled and the bridge reloaded with zero active goals
- after that bridge restart, a fresh human turn ran `sleep 20`; authenticated Dashboard task `173ee17e-1322-4cc4-874a-bc441c13e956` was recovered as the exact active human turn, logged `(steered)`, and returned `HUMAN-PILOT-DONE PILOT-STEER-1786185930-ACK` to Dashboard
- final Hub status: idle, `agent-node:codex-app-server`, model `gpt-5.6-sol`, project `/home/vansin/p-station`, pending inbox 0
- final goal state: active 0, cancelled 2

Pilot verdict: PASS. Fleet rollout remains one node at a time; the known
agent-to-agent single-egress/lifecycle follow-up is recorded separately and is
not represented as fixed by this normalizer.
