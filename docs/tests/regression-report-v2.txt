=== test9 ===
Results: 10 passed, 3 failed
Failures:
- member send_task rejected
- owner send_task rejected
- network quota not enforced

Analysis:
- `viewer send_task rejected` 已转为通过，说明 viewer 写权限拦截生效。
- `member/owner send_task rejected` 更像测试脚本预期过时，不是新回归：
  test9 仍然用 `OWNER_TOKEN` / `MEMBER_TOKEN`（login 得到的 utok_）直接调 MCP `send_task`。
  本轮修复明确把 utok_ 的 MCP 写权限封掉，所以这两个用例应改成：
  1. 走 REST；或
  2. 先换取 ntok_ 再调 MCP。
- quota 用例也是脚本问题：
  test9 第一个注册用户 `owner` 自动是 admin，而 admin 在当前实现里是无限配额。
  脚本却仍用 `OWNER_TOKEN` 做 quota 断言，所以 `network quota not enforced` 不能据此判代码错误。

=== test10 ===
Results: 11 passed, 4 failed
Failures:
- utok unexpectedly allowed MCP tool
- ntok wrote into other network
- ntok did not land in bound network
- ntok can list unrelated networks

Analysis:
- 这 4 个失败仍然是代码侧问题，不是脚本问题。
- test10 说明以下边界还没有真正收紧：
  1. utok_ 仍可写 MCP
  2. ntok_ 仍可越网写 session
  3. ntok_ 落库网络仍不对
  4. ntok_ 读 `/api/networks` 仍能看到非绑定网络
