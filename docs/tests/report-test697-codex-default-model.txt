# test697 — Codex default model migration

SOURCE_COMMIT=685fc7b002a4195fc1a2504e10cee573dd0de1af
BASE_COMMIT=17b8223f9d7fd25fcc435b40e7fa1fc0823ea1de
DATE=2026-08-12 Asia/Shanghai

Scope
-----
- Centralize the supported Codex default as gpt-5.6-sol.
- Apply it to non-interactive create, the actual interactive picker, help text,
  and agent-node startup resolution while preserving explicit --model values.
- Production-source denominator for the retired literal gpt-5.5 is zero.
- This change does NOT add or accept codex-app-server-sdk as a persisted
  runtime spelling. Strict runtime boundaries and RUNTIME_MAP are unchanged.

Exact-source Docker evidence
----------------------------
Image: anet-test697:685fc7b0
Image ID: sha256:db3540b0b575a4d9c3a8dd31f74ec5ee5ec3a051fc39853e09a99f96770484d0
Image env: TEST697_SOURCE_COMMIT=685fc7b002a4195fc1a2504e10cee573dd0de1af
All 18 files changed from BASE_COMMIT through SOURCE_COMMIT matched the
corresponding /workspace file inside the image by SHA-256. No runner-log hash is
claimed as independently reproducible: the log contains date and mktemp paths.

Main layered suite: PASS
- L0: 35 tests, 0 failures, 52 assertions.
- L1: retired-model production denominator is zero.
- L2: real agent-node bundle help advertises gpt-5.6-sol.
- L3: isolated real Hub starts and registers a test user.
- L4: real non-interactive node create writes gpt-5.6-sol.
- L5: explicit operator model remains byte-authoritative in config.json.
- L6: real agent-node startup output reports the default and explicit model.
- L7: a real PTY runs the production CLI and production @inquirer/prompts IO.
  Five Down keys select the visible Codex row; Enter accepts it, then a second
  Enter accepts the model row actually preselected by inquirer. The resulting
  config.json is codex-sdk/gpt-5.6-sol. No internal picker probe or dependency-
  injected selection seam remains in production code.

Seven non-vacuous witnessed-red mutations: PASS
- default-regressed -> L4 real Hub create/config readback turns red.
- picker-default-regressed -> reverses the production default-order comparator;
  the real PTY picker writes o3 and L7 turns red.
- picker-display-order-reversed -> reverses choices inside production
  askChoice(); real inquirer visibly preselects o3, config readback turns red.
- picker-vendor-value-miswired -> maps a displayed vendor row to runtime rather
  than vendor key; the real wizard falls off the Codex path and L7 turns red.
- explicit-model-overwritten -> L5 real Hub create/config readback turns red.
- help-advertises-retired-default -> L2 real bundle/help probe turns red.
- startup-label-regressed -> L6 real bundle/startup-output probe turns red.
Each mutation has a byte-change guard and is judged by its named product layer.
All three picker mutations die at the real PTY/config-readback L7, not in a
source-string, sibling helper, or injected selection seam.

Codex app-server timeout regression evidence on the same exact image: PASS
- runtime.test.ts: 23 tests, 0 failures, 59 assertions.
- app-server client/bridge/session-manager: 60 tests, 0 failures,
  224 assertions.
- Covered behavior includes a distinct finite queue deadline, response timeout
  armed only after exact task start, exact task activity renewal, authoritative
  thread/read recovery of missed terminal events, FIFO drain, and no attribution
  from another task. This is evidence for the existing current-main fix; it does
  not claim that changing the default model alone repairs old deployed runtimes.

Honest limits
-------------
- The five resolveCodexModel(MODEL) vendor-call sites are not exercised against
  a real Codex vendor in this Docker suite. Their real-vendor wire result is
  NOT COVERED; L6 proves startup resolution/labeling, not vendor acceptance.
- No production Hub, token, database, node, or fleet migration was touched.
- PTY keystrokes intentionally depend on the current production vendor order:
  Intern is first and Codex is five Down keys away. A vendor-list reorder must
  update the explicit operator journey rather than silently changing its target.

Result: PASS
Production deployment/fleet migration: NOT PERFORMED.
