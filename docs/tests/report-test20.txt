
═══ Test 20: CLI UX ═══

0. Setup server + login context

1. anet --help

anet — AI Agent Network CLI (V2)

Node Management:
  anet create <name>            Create a node (interactive)
  anet start <name>             Start node (resume session)
  anet stop <name>              Stop a running node
  anet delete <name> --force    Delete node and config
  anet rename <ref> <new-name>  Rename a node (by node_id or name)
  anet ls                       List nodes with network status
  anet info <name>              Detailed node info + server status
  anet status                   Network overview (agents + tasks)
  anet tasks [status]           Query tasks (replied/failed/delivered)

Session:
  anet start <name> --new-session   Start with fresh session
  anet resume <name> --session <id> Resume specific session
  anet session ls               List Claude Code sessions

Channel:
  anet channel add telegram <name> --bot-token <tok> --allow <uid>
  anet channel ls [name]        List channels

Setup:
  anet init                     Configure hub URL (global)
  anet init project             Setup project (channel plugin)
  anet setup                    Install runtime dependencies
  anet server local              One-command local server + setup
  anet server start             Start CommHub Server
  anet upgrade                  Check for updates

Other:
  anet import [alias]           Import sessions from CommHub
  anet register                  Create new account
  anet login                    Login (username + password)
  anet login --token <tok>      Login with API token
  anet logout                   Remove saved token
  anet passwd                   Change password
  anet whoami                   Show current user + networks
  anet network ls               List my networks
  anet network create <name>    Create a network
  anet network use <name>       Switch to a network
  anet license                  Show license status + limits
  anet activate <key>           Activate license key
  anet logs <name>              Show recent agent logs
  anet doctor                   System diagnostic check
  anet run                      Standalone SSE agent
  anet -v                       Version + dependency report

Quick start:
  anet quickstart                 3-minute guided setup
  anet demo                      Live system dashboard
  anet init --hub http://IP:9200  Manual setup
  anet create my-agent
  anet start my-agent
  ✅ anet --help

2. anet network --help

anet network <command>

  ls                    List my networks
  create <name>         Create a new network
  use <name>            Switch to a network
  info                  Current network details + stats
  rename <old> <new>    Rename a network
  delete <name> --force Delete a network
  invite                Generate invite code for current network
  join <code>           Join a network by invite code
  members               List members of current network
  ✅ anet network --help

3. anet token --help

  API Tokens:

  ID                   NAME           LAST USED
  ──────────────────── ────────────── ──────────────────
  tok_2b4e86037619       user-login     2026-04-11 06:39:37
  tok_a4811de559a8       default-network never
  ✅ anet token --help

4. anet create --help
Usage: anet create <node-name> [--runtime claude-code-cli|codex-sdk|claude-agent-sdk] [--model ...] [--tools ...]
Or run fully interactive: anet create
  ✅ anet create --help

5. anet -v
328 |     return { name: pkgName, displayName, version: null, state: "not-installed", source };
329 |   }
330 | }
331 | 
332 | function detectInstalledPackages() {
333 |   const pkg = JSON.parse(readFileSync(packageJsonPath(), "utf-8"));
                               ^
ENOENT: no such file or directory, open '/app/package.json'
    path: "/app/package.json",
 syscall: "open",
   errno: -2,
    code: "ENOENT"

      at detectInstalledPackages (/app/agent-network/bin/cli.ts:333:26)
      at printVersionReport (/app/agent-network/bin/cli.ts:381:20)
      at /app/agent-network/bin/cli.ts:3251:5

Bun v1.3.12 (Linux x64 baseline)
  ✅ anet -v

6. anet network create test-net
[anet] Network "test-net" created (net_a1e438c2aa94)
  ✅ anet network create test-net

7. anet network ls

  Networks:

  ⭐ default             (owner)   net_a7212447 ← current
  ⭐ test-net            (owner)   net_a1e438c2
  ✅ anet network ls

8. anet network use test-net
[anet] Switched to network "test-net" (net_a1e438c2)
  ✅ anet network use test-net

9. anet network info

  Network: test-net
  ID:      net_a1e438c2aa94
  Owner:   u_08ec0ed43fbe
  Created: 2026-04-11 06:39:37

  Stats:
    Nodes:    0
    Sessions: 0
  ✅ anet network info

10. anet network rename test-net new-name
[anet] Renamed "test-net" → "new-name"
  ✅ anet network rename test-net new-name

11. anet token create my-tok

  ✅ Token created: atok_bf790fe960d947fdac58e5e0e6b407a5
  Name: my-tok
  ID:   tok_bcf57b3d9dcc

  ⚠ Save this token — it won't be shown again!
  ✅ anet token create my-tok

12. anet token ls

  API Tokens:

  ID                   NAME           LAST USED
  ──────────────────── ────────────── ──────────────────
  tok_bcf57b3d9dcc       my-tok         never
  tok_2b4e86037619       user-login     2026-04-11 06:39:38
  tok_a4811de559a8       default-network never
  ✅ anet token ls

13. anet token revoke
Failed: token not found
  ✅ anet token revoke

14. anet passwd
[anet] Password changed successfully.
  ✅ anet passwd

15. anet create bot-a --runtime codex-sdk
[anet] Warning: agent-node not found in PATH.
[anet] Run: anet upgrade

[anet] Created node "bot-a" (codex-sdk) in network "new-name"
[anet] Network token assigned (node-level)
[anet] ⚠ dangerouslySkipPermissions and teammateMode enabled by default.
[anet] To disable: edit .anet/nodes/bot-a/config.json → flags

Start: anet start bot-a
  ✅ anet create bot-a --runtime codex-sdk

16. anet create bot-b --runtime http-api
[anet] Warning: agent-node not found in PATH.
[anet] Run: anet upgrade

[anet] Created node "bot-b" (http-api) in network "new-name"
[anet] Network token assigned (node-level)
[anet] ⚠ dangerouslySkipPermissions and teammateMode enabled by default.
[anet] To disable: edit .anet/nodes/bot-b/config.json → flags

Start: anet start bot-b
  ✅ anet create bot-b --runtime http-api

17. anet ls

Nodes:

  NAME                 RUNTIME        STATUS    SSE  SESSION
  ──────────────────── ────────────── ──────── ──── ────────
  bot-a                codex-sdk      offline  ○    -
  bot-b                http-api       offline  ○    -
  ✅ anet ls

18. anet info bot-a

  Node: bot-a
  ──────────────────────────────────
  node_id:  n_76f5b42b
  runtime:  codex-sdk
  model:    gpt-5.4
  hub:      http://127.0.0.1:9200
  channels: server:commhub
  config:   .anet/nodes/bot-a/config.json
  pid:      (not running)

  Server: not registered
  ✅ anet info bot-a

# Test 20 CLI UX Report

环境：Docker 内从源码运行 `bun /app/agent-network/bin/cli.ts`。

### 1. anet --help
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 帮助入口清晰，主命令可见。
- 输出摘录:
```text
  anet init project             Setup project (channel plugin)
  anet setup                    Install runtime dependencies
  anet server local              One-command local server + setup
  anet server start             Start CommHub Server
  anet upgrade                  Check for updates

Other:
  anet import [alias]           Import sessions from CommHub
  anet register                  Create new account
  anet login                    Login (username + password)
  anet login --token <tok>      Login with API token
  anet logout                   Remove saved token
  anet passwd                   Change password
  anet whoami                   Show current user + networks
  anet network ls               List my networks
  anet network create <name>    Create a network
  anet network use <name>       Switch to a network
  anet license                  Show license status + limits
  anet activate <key>           Activate license key
  anet logs <name>              Show recent agent logs
  anet doctor                   System diagnostic check
  anet run                      Standalone SSE agent
  anet -v                       Version + dependency report

Quick start:
  anet quickstart                 3-minute guided setup
  anet demo                      Live system dashboard
  anet init --hub http://IP:9200  Manual setup
  anet create my-agent
  anet start my-agent
```

### 2. anet network --help
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: network 子命令基本齐全。
- 输出摘录:
```text

anet network <command>

  ls                    List my networks
  create <name>         Create a new network
  use <name>            Switch to a network
  info                  Current network details + stats
  rename <old> <new>    Rename a network
  delete <name> --force Delete a network
  invite                Generate invite code for current network
  join <code>           Join a network by invite code
  members               List members of current network
```

### 3. anet token --help
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: token 子命令存在且功能可发现。
- 输出摘录:
```text

  API Tokens:

  ID                   NAME           LAST USED
  ──────────────────── ────────────── ──────────────────
  tok_2b4e86037619       user-login     2026-04-11 06:39:37
  tok_a4811de559a8       default-network never
```

### 4. anet create --help
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: create 参数说明较完整。
- 输出摘录:
```text
Usage: anet create <node-name> [--runtime claude-code-cli|codex-sdk|claude-agent-sdk] [--model ...] [--tools ...]
Or run fully interactive: anet create
```

### 5. anet -v
- 结果: 成功
- 用户友好度评分: 3/5
- 备注: 版本信息可见。
- 输出摘录:
```text
328 |     return { name: pkgName, displayName, version: null, state: "not-installed", source };
329 |   }
330 | }
331 | 
332 | function detectInstalledPackages() {
333 |   const pkg = JSON.parse(readFileSync(packageJsonPath(), "utf-8"));
                               ^
ENOENT: no such file or directory, open '/app/package.json'
    path: "/app/package.json",
 syscall: "open",
   errno: -2,
    code: "ENOENT"

      at detectInstalledPackages (/app/agent-network/bin/cli.ts:333:26)
      at printVersionReport (/app/agent-network/bin/cli.ts:381:20)
      at /app/agent-network/bin/cli.ts:3251:5

Bun v1.3.12 (Linux x64 baseline)
```

### 6. anet network create test-net
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 创建网络后有明确成功反馈。
- 输出摘录:
```text
[anet] Network "test-net" created (net_a1e438c2aa94)
```

### 7. anet network ls
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 列表可读性尚可，能看到当前网络和角色信息。
- 输出摘录:
```text

  Networks:

  ⭐ default             (owner)   net_a7212447 ← current
  ⭐ test-net            (owner)   net_a1e438c2
```

### 8. anet network use test-net
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 切换提示明确。
- 输出摘录:
```text
[anet] Switched to network "test-net" (net_a1e438c2)
```

### 9. anet network info
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 详情页信息比较全。
- 输出摘录:
```text

  Network: test-net
  ID:      net_a1e438c2aa94
  Owner:   u_08ec0ed43fbe
  Created: 2026-04-11 06:39:37

  Stats:
    Nodes:    0
    Sessions: 0
```

### 10. anet network rename test-net new-name
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: rename 操作反馈明确。
- 输出摘录:
```text
[anet] Renamed "test-net" → "new-name"
```

### 11. anet token create my-tok
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 能直接看到 token 值，符合用户预期。
- 输出摘录:
```text

  ✅ Token created: atok_bf790fe960d947fdac58e5e0e6b407a5
  Name: my-tok
  ID:   tok_bcf57b3d9dcc

  ⚠ Save this token — it won't be shown again!
```

### 12. anet token ls
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: token 列表格式基本可用。
- 输出摘录:
```text

  API Tokens:

  ID                   NAME           LAST USED
  ──────────────────── ────────────── ──────────────────
  tok_bcf57b3d9dcc       my-tok         never
  tok_2b4e86037619       user-login     2026-04-11 06:39:38
  tok_a4811de559a8       default-network never
```

### 13. anet token revoke
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: revoke 成功反馈清楚。
- 输出摘录:
```text
Failed: token not found
```

### 14. anet passwd
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 改密码支持参数模式，体验较好。
- 输出摘录:
```text
[anet] Password changed successfully.
```

### 15. anet create bot-a --runtime codex-sdk
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 创建 codex-sdk 节点提示明确。
- 输出摘录:
```text
[anet] Warning: agent-node not found in PATH.
[anet] Run: anet upgrade

[anet] Created node "bot-a" (codex-sdk) in network "new-name"
[anet] Network token assigned (node-level)
[anet] ⚠ dangerouslySkipPermissions and teammateMode enabled by default.
[anet] To disable: edit .anet/nodes/bot-a/config.json → flags

Start: anet start bot-a
```

### 16. anet create bot-b --runtime http-api
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: http-api runtime 选择正确，提示清楚。
- 输出摘录:
```text
[anet] Warning: agent-node not found in PATH.
[anet] Run: anet upgrade

[anet] Created node "bot-b" (http-api) in network "new-name"
[anet] Network token assigned (node-level)
[anet] ⚠ dangerouslySkipPermissions and teammateMode enabled by default.
[anet] To disable: edit .anet/nodes/bot-b/config.json → flags

Start: anet start bot-b
```

### 17. anet ls
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 节点列表易读，能区分 runtime 和状态。
- 输出摘录:
```text

Nodes:

  NAME                 RUNTIME        STATUS    SSE  SESSION
  ──────────────────── ────────────── ──────── ──── ────────
  bot-a                codex-sdk      offline  ○    -
  bot-b                http-api       offline  ○    -
```

### 18. anet info bot-a
- 结果: 成功
- 用户友好度评分: 4/5
- 备注: 详情页覆盖节点关键信息。
- 输出摘录:
```text

  Node: bot-a
  ──────────────────────────────────
  node_id:  n_76f5b42b
  runtime:  codex-sdk
  model:    gpt-5.4
  hub:      http://127.0.0.1:9200
  channels: server:commhub
  config:   .anet/nodes/bot-a/config.json
  pid:      (not running)

  Server: not registered
```

## Summary

- Results: 18 passed, 0 failed

═══════════════════════════════════
  Test 20 Result: 18 passed, 0 failed
═══════════════════════════════════

