# test698 — capability-negotiated atomic peer reply

Date: 2026-08-12 Asia/Shanghai
Base: 17b8223f9d7fd25fcc435b40e7fa1fc0823ea1de
SOURCE_COMMIT: 04731f5d89c15b014709b2726df761d9f081c9d8
Scope: Hub + agent-node protocol/runtime wiring + isolated Docker tests.
Production deployment: NOT PERFORMED.

## Exact-source image

- image: `anet-test698:04731f5d`
- image id: `sha256:5d4b3b2e17c5d74a19731fbb00b4eaac9098e72bd98458b9d7d10c4d0796c246`
- image env: `TEST698_SOURCE_COMMIT=04731f5d89c15b014709b2726df761d9f081c9d8`
- all nine files changed since the prior frozen source and copied into the
  image matched `git show SOURCE_COMMIT:<path>` byte-for-byte: 9/9.
- focused result: 177 pass / 0 fail / 518 expect calls.
- local main-run artifact digest:
  `sha256:915cf9a89d2ffe1e47adcfb19ca7a6ac3db3761a45e57a21f4d55ad7ca1305d8`.
  This digest records this run only; timestamps and random IDs make it
  intentionally non-reproducible and it is not used as provenance.

## Product behavior evidence

1. `send_peer_reply` performs one synchronous SQLite transaction containing
   exact node-token ownership validation, terminal task update, scheduled-run
   terminalization, and one `requires_response=none` inbox result. Injected
   task/run failures roll all three data surfaces back.
2. Recipient capability is current-session authority only: every token-bound
   heartbeat writes 1 or 0, historical node snapshots are not authoritative,
   and a live SSE connection is required.
3. A task dispatched before node-id/token rotation receives an atomic
   `reply_task_not_owned` with zero writes, then exactly one marked legacy
   task. A wrong reply target remains a hard error and never falls back.
4. Origin kind is decided from the persisted task before caller capability.
   Real-process fixtures prove both a bound and a deliberately unbound
   receiver ntok answer a Dashboard-origin task (`from_node_id=NULL`) through
   `send_reply`: the original becomes `replied`, exactly one Dashboard reply
   is stored, and zero child task rows are created.
5. The real `agent-node/src/cli.ts` ran against an isolated real Hub with a
   deterministic SDK preload. A terminal peer reply entered the runtime and
   was ACKed without reply egress; disabling SSE wake or restoring automatic
   terminal-reply egress makes the real-process gate fail.
6. Capability/identity downgrade remains observable through a legacy task
   carrying `peer_reply_legacy_fallback=true` and a production-computed reason.
   Ambiguous transport and roster failures remain pending rather than choosing
   a potentially lossy fallback.

## Real old-Hub wire and roster routing

`run-legacy-wire.sh` archives and builds the actual base
`17b8223f9d7fd25fcc435b40e7fa1fc0823ea1de`, starts it on an isolated Docker
network, and sends a real MCP `tools/call` for its absent `send_peer_reply`.
The old Hub returned its real wire form:

`result.isError: MCP error -32602: Tool send_peer_reply not found`

The exact-source sender then queried that same old Hub's real `get_all_status`
roster. Its token-bound `dispatcher` session routed once through `send_task`;
the non-session `admin` target routed once through `send_reply`.

Result:
`LEGACY_HUB_WIRE_PASS code=-32602 roster_agent=task roster_user=reply`

A byte-changing source mutation hardcoded every old-Hub target as an agent,
rebuilt a separate image, and failed this exact old-Hub wire test:
`MUTATION_RED: old-hub-roster-route-hardcoded rc=1`.

Local old-Hub artifact digest:
`sha256:026fdb1fab7d1fb2eaebe473a0bfc276548184322129eb4298476081ba1fb945`.
It records this run only and is not used as reproducible provenance.

## Witnessed-red mutations

All mutations require a byte change; a no-op mutation fails the runner. The
main exact-source image produced 22 named reds, plus the real-old-Hub source
mutation above (23 total):

- cli-reply-type-lost
- cli-terminal-reply-egress-restored
- cli-new-reply-wake-disabled
- cli-fallback-reason-lost
- dashboard-origin-misrouted-to-task
- dashboard-origin-checked-after-capability
- terminal-reply-replies-again
- new-reply-wake-removed
- old-hub-fallback-removed
- legacy-mcp-code-lost
- identity-rotation-fallback-removed
- capability-provenance-bypassed
- recipient-capability-gate-removed
- rollback-capability-clear-removed
- legacy-fallback-marker-removed
- reply-rename-canonicalization-removed
- node-ownership-check-removed
- task-terminalization-removed
- reply-requires-response
- scheduled-run-sync-removed
- capability-advertisement-removed
- legacy-agent-warning-removed
- old-hub-roster-route-hardcoded (separate exact old-Hub runner)

The first six mutate production `cli.ts` or `server/src/tools.ts` and rerun
the real-process behavior E2E. In particular, moving Dashboard origin
classification behind capability makes the deliberately unbound ntok fixture
fail instead of terminalizing the task. The final mutation rebuilds production
sender code and exercises the actual old Hub response and roster; it is not a
unit-authored protocol fixture.

## Deployment and honest limits

- No production DB, token, node, runtime, or fleet process was read or changed.
- No live fleet rollout, production traffic cost measurement, or long-duration
  SSE outage test was performed.
- Old-Hub target classification uses an exact live roster lookup on every
  fallback. No negative roster result is cached. If the roster is absent or
  unreadable, the reply stays pending; the sender does not guess.
- Mixed-version fallback intentionally preserves the old two-task shape for
  node-to-node replies. Its metadata makes that residue countable. Dashboard
  and other non-session targets use terminal `send_reply` instead.
- SSE emission occurs after transaction commit. Reconnect drains missed inbox
  rows, but there is no independent periodic polling guarantee.
