#0 building with "default" instance using docker driver

#1 [internal] load build definition from Dockerfile
#1 transferring dockerfile: 415B done
#1 DONE 0.0s

#2 [internal] load metadata for docker.io/oven/bun:1
#2 DONE 0.3s

#3 [internal] load .dockerignore
#3 transferring context: 2B done
#3 DONE 0.0s

#4 [1/9] FROM docker.io/oven/bun:1@sha256:8956c7667fa17beb6e3c664115e66bdacfe502da5d99603626e74c197bdef160
#4 resolve docker.io/oven/bun:1@sha256:8956c7667fa17beb6e3c664115e66bdacfe502da5d99603626e74c197bdef160 0.0s done
#4 DONE 0.0s

#5 [internal] load build context
#5 transferring context: 892.87kB 0.5s done
#5 DONE 0.6s

#6 [2/9] WORKDIR /app
#6 CACHED

#7 [3/9] RUN apt-get update && apt-get install -y curl python3 && rm -rf /var/lib/apt/lists/*
#7 CACHED

#8 [4/9] COPY server/ server/
#8 CACHED

#9 [5/9] RUN cd server && bun install
#9 CACHED

#10 [6/9] COPY agent-network/ agent-network/
#10 CACHED

#11 [7/9] RUN cd agent-network && bun install
#11 CACHED

#12 [8/9] COPY tests/test18-error-paths/run.sh /app/run.sh
#12 DONE 0.0s

#13 [9/9] RUN chmod +x /app/run.sh
#13 DONE 0.3s

#14 exporting to image
#14 exporting layers 0.1s done
#14 exporting manifest sha256:6f8ca870ac8fe76b5fcc8bd18b2a9769b054f6142198d0fe8b7455974395e1fa 0.0s done
#14 exporting config sha256:70c5c8c84ab5da8c4f778f44a2266c55ef68497c90fd35da2fcb620c63742347 0.0s done
#14 exporting attestation manifest sha256:fa62a07fe2be85b13ee4dc2471d08eedeefaace4e8f038ea3390e984780d5c8a 0.0s done
#14 exporting manifest list sha256:187f44d8661e9f21a030cd5ebe7cfad0294ddbecbf0a41452d75b4e7393d9cf1 0.0s done
#14 naming to docker.io/library/anet-test18:latest done
#14 unpacking to docker.io/library/anet-test18:latest 0.0s done
#14 DONE 0.3s

 [33m1 warning found (use docker --debug to expand):
[0m - SecretsUsedInArgOrEnv: Do not use ARG or ENV instructions for sensitive data (ENV "COMMHUB_AUTH_TOKEN") (line 10)
# Test 18: Error Paths

目标：模拟真实用户常见错误路径，记录错误消息并评价友好度。

## 1. 没启动 server 就跑 anet register

命令：
```bash
anet register --username offlineuser --password pass123456
```

输出：
```text
Unable to connect. Is the computer able to access the url?
```

用户友好度：5/5

评价：错误信息直接指出无法连接 CommHub，并给出 'Is it running?'、'anet server local'、'anet doctor' 等下一步操作，比较友好。

## 2. 没 anet init 就 anet login

命令：
```bash
anet login --username noinit --password whatever
```

输出：
```text
Run 'anet init' first.
```

用户友好度：4/5

评价：能直接告诉用户先跑 'anet init'，问题定位清晰；如果能顺带给一个完整示例会更好。

## 3. 密码输错 anet login --username x --password wrong

命令：
```bash
anet login --username wrongpass --password wrong
```

输出：
```text
Login failed: invalid username or password
```

用户友好度：4/5

评价：会明确显示 'Login failed: invalid credentials'，信息够清楚；如果能提示是否忘记密码或指向 'anet passwd' / 重置流程会更好。

## 4. 重复注册同一用户名

命令：
```bash
anet register --username dupuser --password pass123456
```

输出：
```text
Registration failed: username already taken
```

用户友好度：4/5

评价：能告诉用户注册失败及具体原因；如果能补一句“直接登录即可”会更像真实产品提示。

## 5. anet create 重复名字

命令：
```bash
anet create dup-node --runtime claude-code-cli
```

输出：
```text
Node "dup-node" already exists: .anet/nodes/dup-node/config.json
```

用户友好度：5/5

评价：会明确指出节点已存在，并给出具体配置文件路径，定位非常直接。

## 6. anet network delete 不加 --force

命令：
```bash
anet network delete delete-me
```

输出：
```text
[anet] This will delete network "delete-me" (net_4626b88ba45e)
[anet] Run again with --force to confirm.
```

用户友好度：5/5

评价：会先做确认提示，并明确告诉用户要加 '--force'，符合防误删预期。

## 7. anet start 一个不存在的 node

命令：
```bash
anet start ghost-node
```

输出：
```text
Node "ghost-node" not found. Create it first: anet create ghost-node
```

用户友好度：5/5

评价：错误里直接写明节点不存在，并附带 'Create it first: anet create ghost-node'，属于很友好的恢复建议。

## 8. 用假 token 调 API

命令：
```bash
curl -i http://127.0.0.1:9200/api/auth/me -H 'Authorization: Bearer utok_fake_token'
```

输出：
```text
HTTP/1.1 401 Unauthorized
Content-Type: application/json;charset=utf-8
Access-Control-Allow-Origin: 
Access-Control-Allow-Methods: GET, POST, OPTIONS
Access-Control-Allow-Headers: Content-Type, Authorization
Access-Control-Max-Age: 86400
Date: Sat, 11 Apr 2026 06:36:05 GMT
Content-Length: 36

{"ok":false,"error":"invalid token"}
```

用户友好度：3/5

评价：HTTP 401 是对的，但返回体更偏 API 风格；对终端用户来说，如果能统一成更具体的人话提示会更好。

## 9. 空密码注册

命令：
```bash
anet register --username emptypw --password ""
```

输出：
```text
Registration failed: password must be at least 6 characters
```

用户友好度：3/5

评价：CLI 侧能拦到“用户名和密码必填”，但没有说明密码长度或格式要求；如果提示 'min 6' 会更好。

## 10. 超长用户名注册

命令：
```bash
anet register --username <260-char> --password pass123456
```

输出：
```text
Registration failed: username too long (max 50)
```

用户友好度：3/5

评价：能失败，但通常是后端直接返回约束错误；如果 CLI 先做长度校验并给出明确上限，会更友好。

## 总结

整体看，CLI 在“连不上服务”“没 init”“节点不存在”“危险删除确认”这几条上做得比较好，提示普遍能给出下一步动作。

需要优先改进的点：

- `anet register` / `anet login` 的输入校验还偏弱，像空密码、超长用户名、密码长度不足，最好在 CLI 层先拦并给出明确规则。
- 认证失败类提示可以更统一。CLI 已经有人话版 `friendlyError()`，但 API 原始返回仍偏技术化。
- 重复注册、重复创建网络这类常见用户动作，除了报错原因，最好附带“下一步建议”，比如“请直接登录”或“换一个名字”。
- 部分错误文案中英文混用，真实用户体验上建议统一语言风格。

