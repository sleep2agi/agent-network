# Test evidence index

Most reports in this directory are generated by isolated Docker suites. Manual
protected gates are explicitly marked `NOT-IN-CI` and must never commit copied
credentials, disposable runtime homes, or raw traces.

## Codex co-presence

- [Linux real Codex 0.148 protected E2E](report-test1193-linux-real-codex-0148-e2e.txt)
  — evidence-only, `NOT-IN-CI`; records the Docker prerequisites, real host
  outcome, restart/history recovery, production before/after guard, binary
  hashes, and the boundary between fake-wire RPC counts and real-bridge logs.
