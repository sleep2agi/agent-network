// RFC-030 Wave 1A P0.2 Commit 2 corrective round 10 (副指挥
// 9a9a198d). Positive baseline: correct `safeAdoptConsume`
// usage. Must typecheck CLEAN. The harness's meta-sanity
// asserts this.

import { safeAdoptConsume } from "../safe-adopt";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const use = (): void => {
  safeAdoptConsume(
    Promise.resolve("v"),
    (_v: unknown): undefined => { return undefined; },
    (_reason: unknown): undefined => { return undefined; },
    (_cbErr: unknown): undefined => { return undefined; },
  );
};
